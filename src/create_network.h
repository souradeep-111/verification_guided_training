#ifndef run_python_h
#define run_python_h

// Some function which create a randomly initialized neural network, and saves it to a file
// with the name of the file received

// Some function which runs python script according to the points received,
// trains a neural network from it according to some MPC setting

#endif
