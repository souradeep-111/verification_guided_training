#ifndef property_h
#define property_h

// The only property that we have in mind right now is the target reach specification

// Some function which takes in a point, and says up to what degree that property is being
// satisfied,

// Some function which given a point, a neural network, and can tell what is the gradient of that point with respect
// to some property.


#endif
