#ifndef generate_counter_examples_h
#define generate_counter_examples_h

// Some function which takes in the neural network and a property,
// pairs that that with the searching of counter examples

#endif
